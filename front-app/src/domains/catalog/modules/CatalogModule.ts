export const handleSearch = (_query: string): void => {
	console.log('Search:', _query);
};

export const handleIngredientFilter = (_ingredients: string[]): void => {
	console.log('Ingredient filter:', _ingredients);
};
