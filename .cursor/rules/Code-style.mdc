---
alwaysApply: true
---

- Внутри react-комопнентов создавай функции через function. Boolean-пропсы всегда перевадай явно field={true}
- Для каждого компонента всегда создаётся одноимённая директория, в которой должен быть сам компонент, а также могут быть scss-файлы и прочее
